import smtplib

server = smtplib.SMTP( "smtp.gmail.com", 587 )
server.starttls()
server.login( 'createEmail@gmail.com', 'yourEmailPass' )
from_mail = 'createEmail@gmail.com'
to = 'phoneNum@messaging.sprintpcs.com'
body = '<ALARM>'
message = ("From: %s\r\n" % from_mail + "To: %s\r\n" % to + "Subject: %s\r\n" % '' + "\r\n" + body)
server.sendmail(from_mail, to, message)
print("ran");